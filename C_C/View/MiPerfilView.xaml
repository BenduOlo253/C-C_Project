<Window x:Class="C_C.View.MiPerfilView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C_C.View"
        
        xmlns:vm="clr-namespace:C_C.ViewModel"
        mc:Ignorable="d"
        Title="C&amp;C App - Mi Perfil" Height="800" Width="450"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        
        x:Name="MiPerfilWindow">

    <Window.Resources>
        
        <SolidColorBrush x:Key="FondoPrincipal" Color="#001f3f"/>
        <SolidColorBrush x:Key="HeaderFondo" Color="#0097b2"/>
        <SolidColorBrush x:Key="FondoClaro" Color="#F0F8FF"/>
        <SolidColorBrush x:Key="TextoGris" Color="#555"/>
        <SolidColorBrush x:Key="TextoTitulo" Color="#001f3f"/>
        <SolidColorBrush x:Key="BordeInput" Color="#B0B0B0"/>

        
        <Style x:Key="BotonIcono" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        
        <Style x:Key="TituloSeccion" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextoTitulo}"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>

        
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource BordeInput}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        
        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="{StaticResource BordeInput}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource FondoPrincipal}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       
        <Border Grid.Row="0" Background="{StaticResource HeaderFondo}" Padding="15,25,15,15">
            <Grid Margin="0,-3,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                
                <Button Grid.Column="0" Style="{StaticResource BotonIcono}" VerticalAlignment="Center" 
                        Command="{Binding VolverCommand}"
                        CommandParameter="{Binding ElementName=MiPerfilWindow}">
                    <Path Data="M 15,0 L 0,15 L 15,30" Stroke="White" StrokeThickness="3" Width="15" Height="18" Stretch="Uniform"/>
                </Button>

                <Image Source="pack://application:,,,/Resources/Images/C_C_LOGO.png" Grid.Column="1" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                
                <Button Grid.Column="2" Style="{StaticResource BotonIcono}" VerticalAlignment="Center"
                        Command="{Binding GuardarCommand}">
                    <Path Data="M 0,10 L 10,20 L 30,0" Stroke="White" StrokeThickness="4" Width="20" Height="15" Stretch="Uniform"/>
                </Button>
            </Grid>
        </Border>

       
        <ScrollViewer Grid.Row="1" Background="{StaticResource FondoClaro}" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">

               
                <Grid HorizontalAlignment="Center">
                    <Ellipse Width="120" Height="120" Fill="LightGray"/>
                   
                    <Border Background="#80000000" Width="30" Height="30" CornerRadius="15" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5">
                        <Button Style="{StaticResource BotonIcono}" Background="Transparent">
                            <Image Source="pack://application:,,,/Resources/Images/Lapiz.png" Width="15" Height="15" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Button>
                    </Border>
                </Grid>

                
                <TextBlock Text="Biografía" Style="{StaticResource TituloSeccion}"/>
                
                <TextBox Text="{Binding PerfilActual.Biografia, Mode=TwoWay}" TextWrapping="Wrap" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"/>

                
                <TextBlock Text="Intereses" Style="{StaticResource TituloSeccion}"/>
                <Border BorderBrush="{StaticResource BordeInput}" BorderThickness="1" Background="White" CornerRadius="5" Padding="5" MinHeight="40">
                    
                    <TextBlock Text="Anime, Videojuegos, Programar..." Foreground="{StaticResource TextoGris}" Padding="5" TextWrapping="Wrap"/>
                </Border>

                
                <TextBlock Text="Ajustes de cuenta" Style="{StaticResource TituloSeccion}"/>
                <TextBlock Text="Correo electrónico" Margin="0,0,0,5" Foreground="{StaticResource TextoGris}"/>
                
                <TextBox Text="{Binding AlumnoActual.Email, Mode=TwoWay}" Margin="0,0,0,10"/>
                <TextBlock Text="Cambiar contraseña" Margin="0,0,0,5" Foreground="{StaticResource TextoGris}"/>
                <PasswordBox Margin="0,0,0,10"/>

               
                <TextBlock Text="Ajustes de descubrimiento" Style="{StaticResource TituloSeccion}"/>
                <TextBlock Text="Mostrarme por carrera:" Margin="0,0,0,5" Foreground="{StaticResource TextoGris}"/>
                <ComboBox Margin="0,0,0,10">
                    <ComboBoxItem Content="Ingeniería de Software" IsSelected="True"/>
                    <ComboBoxItem Content="Todas las carreras"/>
                </ComboBox>

                <TextBlock Text="Mostrarme por género:" Margin="0,0,0,5" Foreground="{StaticResource TextoGris}"/>
                <ComboBox Margin="0,0,0,10">
                    <ComboBoxItem Content="Hombres"/>
                    <ComboBoxItem Content="Mujeres"/>
                    <ComboBoxItem Content="Todos" IsSelected="True"/>
                </ComboBox>

                <TextBlock Text="Rango de edad:" Margin="0,0,0,5" Foreground="{StaticResource TextoGris}"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding PreferenciasActuales.EdadMinima, Mode=TwoWay}" HorizontalContentAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="-" FontSize="18" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBox Grid.Column="2" Text="{Binding PreferenciasActuales.EdadMaxima, Mode=TwoWay}" HorizontalContentAlignment="Center"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

